
# Sources set
SET ( SOURCES
  specificworker.cpp
  specificmonitor.cpp
)

# Headers set
SET ( HEADERS
  specificworker.h
  specificmonitor.h
)

INCLUDE( $ENV{ROBOCOMP}/cmake/modules/opencv2.cmake )

ADD_DEFINITIONS( -I/usr/include/eigen3 -I/usr/local/include/eigen3)
MESSAGE(STATUS, ${LIBS} )

SET (LIBS -L/usr/lib/x86_64-linux-gnu ${LIBS} )

#SET (LIBS ${LIBS} opencv_core opencv_features2d opencv_imgproc opencv_contrib )

ADD_LIBRARY(apriltags STATIC IMPORTED)
SET_TARGET_PROPERTIES(apriltags PROPERTIES IMPORTED_LOCATION /usr/local/lib/libapriltags.a)
SET (STATIC_LIBS ${STATIC_LIBS} apriltags)
