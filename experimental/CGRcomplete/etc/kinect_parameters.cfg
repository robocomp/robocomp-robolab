
KinectParameters = {
  f = 600.0;
  fovH = deg2rad(57.00);
  fovV = deg2rad(46.0); 
  minDepth = 0.4;
  maxDepth = 6.0;
  maxDepthVal = 1500;
  width = 320;
  height = 240;
  pixelLocScale = 1;
  
  loc = vec3(0.22, -0.02, 0.70);
  xRot = deg2rad(1.0);
  yRot = deg2rad(17.0);
  zRot = deg2rad(0.0);
};

PlaneFilteringParameters = {
  maxPoints = 2000;
  numSamples = 200000;
  numLocalSamples = 50;
  maxDepthDiff = 1.8;
  planeSize = 60;
  maxError = 0.03;
  minInlierFraction = 0.80;
  WorldPlaneSize = 2.0;
  numRetries = 2;
};
